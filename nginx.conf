server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  # Serve static files and SPA fallback at site root.
  # The Dockerfile copies built files to /usr/share/nginx/html.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
